import Typed from 'typed.js';

const typedHome = () => {

  const options = {
  strings: ["PharmaDrive, <strong>l'ordonnance</strong>  <strong>de demain</strong>!"],
  typeSpeed: 80
  };

  const typed = new Typed(".homescript", options);
  };

export { typedHome };

function drag_drop(){
  function allowDrop(ev) {
    ev.preventDefault();
  }

  function dontAllowDrop(ev) {
    ev.dataTransfer.dropEffect = "none";
  }

  function drag(ev) {
    ev.dataTransfer.setData("text", ev.target.id);
    const idDrag = ev.target.id
    console.log(idDrag);
    let selectDiv = document.querySelector('.left');
    selectDiv.addEventListener('drop',(event) => {
    window.location.href = `<%= link_to 'Préparer', pharmacy_list_path(${idDrag}, list: {ready_at: Time.now}), method: :patch %>`
})

  }

  function drop(ev) {
    ev.preventDefault();
    var data = ev.dataTransfer.getData("text");
    ev.target.appendChild(document.getElementById(data));
  }


};

export { drag_drop };
